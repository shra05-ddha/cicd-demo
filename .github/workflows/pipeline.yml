name: CI Pipeline Demo

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Run Python Application
      run: python app.py

    - name: Inject Malware
      run: |
        echo 'print("Malicious code injected by attacker")' >> app.py
    
    - name: Use Secure API Key
      run: echo ${{ secrets.API_KEY }}

    - name: Access Cloud Credentials
      run: env

    - name: Modify Deployment
      run: echo "Deploying compromised application"